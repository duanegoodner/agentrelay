# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# pixi environments
.pixi/*
!.pixi/config.toml

# mkdocs build output
site/

# Workflow runtime state — entirely ephemeral, not version-controlled.
# Workflow/graph definitions should live in a top-level directory (e.g. graphs/)
# rather than here, to keep the gitignore simple.
.workflow/

# Sprint Notes — 2026-03-14

> **Status: Complete.**

## Goal

Complete the end-to-end wiring from graph YAML to running orchestrator. All
protocol implementations exist; the remaining work is composition, CLI, and
cleanup.

## Context

The 2026-03-13 sprint built out all protocol implementations and the agent
communication protocol. An unplanned architectural improvement (PR #99) then
replaced `TaskRunnerIO` with per-step `StepDispatch[T]` dispatch tables on a
new `StandardTaskRunner`, promoted `TaskRunner` to a protocol, and moved
workstream context (`integration_branch`, `workstream_worktree_path`) from
implementation constructors into `TaskState`.

### What's done

| PR | What | Date |
|---|---|---|
| #88 | `ops/` infrastructure primitives | 2026-03-12 |
| #91 | Agent communication protocol spec | 2026-03-13 |
| #92 | Protocol schemas, builders, templates (PR K) | 2026-03-13 |
| #96 | Task-level protocol implementations (PR L) | 2026-03-13 |
| #97 | Fix: task preparer uses shared workstream worktree | 2026-03-13 |
| #98 | Workstream-level protocol implementations (PR M) | 2026-03-13 |
| #99 | `StandardTaskRunner` with `StepDispatch` (unplanned) | 2026-03-14 |
| #101 | Wire WorkstreamRunner into Orchestrator (PR N1) | 2026-03-15 |
| #102 | Extract `_OrchestratorRun` (unplanned) | 2026-03-15 |
| #108 | Composition, CLI, and Reset tool (PRs N2+O) | 2026-03-18 |
| #109 | Docs, demo graphs, and E2E testing (PR P) | 2026-03-19 |

---

## PR plan

### PR N1: Wire WorkstreamRunner into Orchestrator — **Done** (PR #101)

- Branch: `feat/orchestrator-workstream-runner`

**Acceptance criteria:**
- [x] Orchestrator accepts `workstream_runner` parameter.
- [x] Workstream prepare/merge/teardown are called at correct lifecycle points.
- [x] `MERGE_READY` intermediate status decouples state detection from merge I/O.
- [x] `active_task_id` removed; active-task constraint derived from task statuses.
- [x] `fail_fast_on_workstream_error` config blocks new workstream preparation.
- [x] `WorkstreamRunner` is a Protocol; `StandardWorkstreamRunner` is the impl.
- [x] All existing orchestrator tests pass (with `NoOpWorkstreamRunner` double).
- [x] Six new lifecycle tests added.
- [x] `pixi run check` passes.

---

### PR N2+O: Composition, CLI, and Reset tool — **Done** (PR #108)

- Branch: `feat/run-graph-cli`
- New modules: `src/agentrelay/run_graph.py`, `src/agentrelay/reset_graph.py`

Originally planned as two separate PRs (N2: composition/CLI, O: reset tool).
Consolidated into one PR because run and reset are a matched pair — shipping
a CLI to run graphs without a way to reset the repo would be risky.

**Scope:**

1. **`run_graph.py`** — Top-level composition + CLI:
   - `run_graph()` async function wires `TaskGraphBuilder`, `build_standard_runner()`,
     new `build_standard_workstream_runner()`, and `Orchestrator`.
   - CLI via `python -m agentrelay.run_graph <graph.yaml>` with flags:
     `--max-concurrency`, `--max-task-attempts`, `--teardown-mode`,
     `--tmux-session`, `--model`, `--dry-run`.
   - Writes `run_info.json` (start HEAD + timestamp) before orchestrator runs.
   - Operational YAML keys (`tmux_session`, `keep_panes`, `model`) popped from
     raw dict before graph parsing; CLI flags override YAML values.

2. **`reset_graph.py`** — Graph reset tool:
   - `plan_reset()` / `execute_reset()` separated for testability.
   - CLI via `python -m agentrelay.reset_graph <graph.yaml>` with `--yes` flag.
   - Closes open PRs, resets main with ancestry safety check, deletes branches,
     removes worktree and workflow directories.
   - Out-of-order reset detection skips main-branch reset if unsafe.
   - Idempotent: re-running after a successful reset is safe.

3. **`build_standard_workstream_runner()`** — New builder in `orchestrator/builders.py`.

4. **New ops primitives** — `git.rev_parse_head`, `git.merge_base_is_ancestor`,
   `git.reset_hard`, `git.push_force_with_lease`, `git.ls_remote_branches`,
   `gh.pr_list`, `gh.pr_close`.

**Acceptance criteria:**
- [x] `--dry-run` validates graph and prints execution plan.
- [x] Integration test verifies full wiring from graph YAML to orchestrator.
- [x] Reset is idempotent when re-run.
- [x] Reset tested against temp git repo fixture (10 tests).
- [x] `pixi run check` passes (833 tests, 0 errors).

---

### PR P: Docs, demo graphs, and E2E testing — **Done** (PR #109)

- Branch: `feat/e2e-docs`

Demo graphs versioned in-repo, E2E testing scripts, conflict detection,
updated docs.

**Acceptance criteria:**
- [x] Documented end-to-end run/reset cycle.
- [x] Docs internally consistent.
- [x] `pixi run check` passes.

---

## Sprint status vs. 2026-03-13 plan

The 2026-03-13 sprint defined PRs K through P. Here's the mapping:

| 2026-03-13 item | Status | Notes |
|---|---|---|
| PR K: Protocol schemas, builders, templates | **Done** (PR #92) | |
| PR L: Task-level protocol implementations | **Done** (PR #96) | |
| Fix-up: Task preparer branch-only | **Done** (PR #97) | |
| PR M: Workstream-level protocol implementations | **Done** (PR #98) | |
| (unplanned) StandardTaskRunner dispatch | **Done** (PR #99) | Architectural improvement |
| PR N1: Wire WorkstreamRunner into Orchestrator | **Done** (PR #101) | |
| (unplanned) Extract `_OrchestratorRun` | **Done** (PR #102) | |
| PR N2: Composition and CLI | **Done** (PR #108) | Consolidated with PR O |
| PR O: Reset tool | **Done** (PR #108) | Consolidated with PR N2 |
| PR P: Docs and E2E validation | **Done** (PR #109) | E2E scripts + demo graphs |

**Consolidation note:** PRs N2 and O were merged into a single PR on branch
`feat/run-graph-cli`. Run and reset are a matched pair — shipping one without
the other would allow "damage" without a quick recovery path.

---

## Merge discipline

1. Complete and merge each PR before starting the next.
2. `pixi run check` must pass before every PR.
3. Each PR includes a `## Test plan` checklist in the PR body.
4. Update `docs/HISTORY.md` with each merged PR.

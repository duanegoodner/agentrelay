# Sprint Notes — 2026-06-07

## Review Findings (non-prototype track, highest impact first)

1. The current architecture layer intentionally stops before orchestration, so the core gap is real runtime execution.  
   [docs/WORKFLOW.md:12](/data/git/agentrelay/main/docs/WORKFLOW.md:12), [docs/WORKFLOW.md:16](/data/git/agentrelay/main/docs/WORKFLOW.md:16), [docs/BACKLOG.md:9](/data/git/agentrelay/main/docs/BACKLOG.md:9)

2. Agent launch/kickoff in the non-prototype layer is still stubbed, so an orchestrator cannot yet dispatch real work there.  
   [src/agentrelay/agent.py:83](/data/git/agentrelay/main/src/agentrelay/agent.py:83), [src/agentrelay/agent.py:116](/data/git/agentrelay/main/src/agentrelay/agent.py:116)

3. There is no non-prototype `TaskGraph`/YAML loader yet; only v01 has that (`AgentTaskGraphBuilder`).  
   [src/agentrelay/prototypes/v01/agent_task_graph.py:103](/data/git/agentrelay/main/src/agentrelay/prototypes/v01/agent_task_graph.py:103)

4. v01 proves the loop, but it is heavily coupled/monolithic; avoid porting it 1:1 into core.  
   [src/agentrelay/prototypes/v01/run_graph.py:697](/data/git/agentrelay/main/src/agentrelay/prototypes/v01/run_graph.py:697), [src/agentrelay/prototypes/v01/task_launcher.py:135](/data/git/agentrelay/main/src/agentrelay/prototypes/v01/task_launcher.py:135)

`pixi run test` passes (`467 passed`), so baseline is clean.

## Your specific questions

- `TaskGraph` next?  
  Yes. Implement it now.

- YAML -> runtime: `TaskGraphBuilder` or just `TaskBuilder`?  
  Use both layers:
  1. `TaskGraphBuilder`: YAML -> validated `TaskGraph` (IDs, deps, cycle checks, defaults).
  2. `TaskRuntimeBuilder` (or factory): `TaskGraph` -> `dict[str, TaskRuntime]`.

- Should `Task` run itself?  
  No. Keep `Task` immutable spec-only ([task.py](/data/git/agentrelay/main/src/agentrelay/task.py:145)). Use `TaskRunner` for execution.

- Implement `Orchestrator`?  
  Yes, but as a thin scheduler over `TaskRuntime`; delegate per-task lifecycle to `TaskRunner`.

## Recommended implementation order (non-prototype)

1. `task_graph.py`: `TaskGraph` + validation/query APIs (`ready`, `dependents`, topo/cycle checks).
2. `graph_builder.py`: YAML schema + `TaskGraphBuilder.from_yaml(...)`.
3. `runtime_builder.py`: initialize `TaskRuntime` objects from graph.
4. `task_runner.py`: one-task lifecycle state machine (`PENDING -> RUNNING -> PR_CREATED -> PR_MERGED/FAILED`).
5. `orchestrator.py`: async dispatch loop + dependency scheduling + resume hooks.
6. Adapter layer for side effects (git/worktree, signals, PR, agent launch), then wire `TmuxAgent` real implementation.

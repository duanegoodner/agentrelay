# Sprint Notes — 2026-03-19

> **Status: In Progress.**

## Goal

Systematically build out end-to-end test coverage by category. Each category
corresponds to a subdirectory under `graphs/` with YAML graphs, README docs,
and observations from test runs. Fix bugs and wire unimplemented features as
needed to enable each test category.

## Context

E2E testing infrastructure is in place (e2e_run.sh, e2e_reset.sh, e2e_check.sh).
Smoke tests exist in `graphs/smoke/`. Real-time console output and verbose mode
were added in PR #112. The dispatch race condition was fixed in PR #111 and the
outcome reporting bug in PR #113.

Investigation revealed several features that are scaffolded (parsed from YAML,
written to policies.json) but not wired in the runner: completion gates, review,
and ADR production. A concerns bug was also found (signal.concerns dropped in
the runner).

### Pre-sprint PRs (completed before sprint doc created)

| PR | What | Date |
|---|---|---|
| #111 | Fix workstream dispatch race condition | 2026-03-19 |
| #112 | E2E graph organization, console output, verbose mode | 2026-03-19 |
| #113 | Fix outcome reporting when workstream merge fails | 2026-03-19 |
| #116 | Diagram catch-up for PRs #111–#115 | 2026-03-20 |

### E2E test categories

| Category | Purpose | Prerequisite |
|----------|---------|-------------|
| `smoke/` | Core paths (exists) | — |
| `concerns/` | Concern capture mechanism | Fix dropped concerns bug |
| `roles/` | Role-specific templates + TaskPaths | — |
| `failure/` | .failed signals, retry, blocked tasks | — |
| `workstreams/` | Diamond, nested, serialization | — |
| `config/` | CLI overrides, teardown modes | — |
| `gates/` | Shell command verification | Wire gate execution |
| `adr/` | ADR verbosity levels | Wire ADR instructions |
| `review/` | Self-review step | Wire review lifecycle |

---

## PR plan

### PR A: Agent-side helper + fix concerns bug + concerns/ test graphs — **Done** (PR #114)

- Branch: `fix/concerns-capture`

**Agent-side helper:**
Create an agent-side helper class that agents instantiate inside their tmux
session to interact with the orchestrator via a clean Python API. Replaces
raw shell commands (`echo ... > .done`) with method calls
(`helper.mark_done(pr_url)`). Reads task metadata from `manifest.json` in
`$AGENTRELAY_SIGNAL_DIR`.

Methods:
- `mark_done(pr_url)` — write .done signal
- `mark_failed(reason)` — write .failed signal
- `record_concern(concern)` — append to concerns.log
- Class method `from_env()` — instantiate from `$AGENTRELAY_SIGNAL_DIR`

Update the workflow footer in `agent_comm_protocol/templates.py` to instruct
agents to use the helper API instead of raw shell commands. Include concern
recording guidance for all roles (optional — "if you have concerns, record
them").

**Bug fix (concerns transfer):**
- `StandardTaskRunner.run()` reads `signal.concerns` from the completion
  checker but never transfers them to `runtime.artifacts.concerns`. Add the
  missing assignment. (Already implemented on `fix/concerns-capture` branch.)

**Test graphs:**
- `graphs/concerns/README.md` — category docs
- Graph with a task whose description instructs the agent to record concerns
  via the helper API. Verify they appear in the orchestrator result.

**Acceptance criteria:**
- [x] Agent-side helper class works when imported by agent in tmux session
- [x] Workflow footer uses helper API for completion signaling and concerns
- [x] Concerns from `concerns.log` are stored in `runtime.artifacts.concerns`
- [x] Concerns appear in console output
- [x] `pixi run check` passes
- [x] E2E: concerns graph runs, agent uses helper, concerns are captured

---

### PR A2: Rich integration PR + stop auto-merging to main — **Done** (PR #115)

- Branch: `feat/integration-pr-summary`

**Stop auto-merging to main:**
- `GhWorkstreamMerger` currently creates the integration PR and immediately
  merges it via `gh pr merge`. Change to create-only — the PR is left open
  for human review and merge. (The prototype also left this to humans.)
- This avoids silent failures from merge conflicts when parallel workstreams
  modify overlapping files.

**Enrich integration PR body:**
- Build a rich PR body that includes:
  - List of tasks in the workstream with descriptions and PR URLs
  - Aggregated design concerns from all tasks
- Pass task summaries to the merger so it can build the body (currently it
  only receives the WorkstreamRuntime with no task-level info).

**Acceptance criteria:**
- [x] Integration PR is created but NOT auto-merged
- [x] Integration PR body lists all tasks with PR links
- [x] Design concerns from all tasks appear in the integration PR body
- [x] `pixi run check` passes
- [x] E2E: integration PR on GitHub has a meaningful summary and is open (not merged)

---

### PR A3: Signal-file-backed TaskStatus (match workstream pattern) — **Done** (PR #117)

- Branch: `feat/task-signal-files`

**Align task status management with the workstream signal file pattern:**
- Establish a task-level signal directory convention under
  `.workflow/<graph>/signals/<task_id>/status/` with files matching
  TaskStatus enum values (pending, running, pr_created, pr_merged, failed).
- Make TaskStatus a computed property on TaskRuntime, derived from which
  signal files exist (same pattern as WorkstreamRuntime after PR A2).
- This should be done soon after PR A2 so that task and workstream status
  management strategies don't diverge for long.

**Acceptance criteria:**
- [x] Task status determined from signal files, same pattern as workstreams
- [x] Existing task lifecycle behavior unchanged
- [x] `pixi run check` passes

---

### PR B1: roles/ pipeline graph + implementer template fix — **Done** (PR #118)

- Branch: `test/role-graphs`

**Delivered:**
- `graphs/roles/pipeline.yaml` — four-role pipeline (spec_writer →
  test_writer → test_reviewer → implementer) building a BoundedQueue class.
  Includes a deliberate spec inconsistency (eviction vs OverflowError) and a
  completion gate on the implementer.
- Updated `templates/implementer.md` to connect concern documentation to
  `helper.record_concern()` with guidance on what qualifies as a concern.
- Backlog items: structured instruction architecture, agent build environment
  awareness (including ops concerns concept and TaskHelper CLI wrapper).

**E2E result:**
- Pipeline runs to completion. All four roles produce expected artifacts.
- Implementer does NOT organically record a concern about the spec
  inconsistency (with Sonnet, baseline instructions). Concern instruction
  refinement deferred to PR BN.

**Acceptance criteria:**
- [x] Each role produces expected artifacts (specs, tests, implementations)
- [x] Multi-role pipeline: changes propagate through the chain
- [ ] ~~Implementer organically records a concern~~ (deferred to PR BN)
- [x] `pixi run check` passes
- [x] E2E: pipeline graph runs to completion

---

### PR B2: Declared tools + TaskHelper CLI + template cleanup — **Done** (PRs #120, #121)

- Branches: `feat/declared-tools-cli`, `fix/template-description-cleanup`

**Delivered:**
- Declared `tools` field in graph YAML (list of tool names). Orchestrator
  validates each tool is available (preflight check) and injects usage
  guidance into agent instructions via `TOOL_REGISTRY` in `tools.py`.
- TaskHelper CLI wrapper: `agentrelay-complete`, `agentrelay-failed`,
  `agentrelay-concern` — shell commands replacing inline Python.
- Workflow footer updated to show CLI commands + tools guidance.
- Optional `$description_section` in all role templates — task description
  now renders directly in instructions.md when present.
- Implementer concern guidance aligned to CLI (`agentrelay-concern`).
- Backlog items: instruction architecture trade-offs, role-specific
  workflow issues (no-commit PR, git push upstream, spec source-of-truth).

**E2E result:**
- Pipeline runs to completion. Agents use `pixi run` and complete via CLI.

**Acceptance criteria:**
- [x] Agents consistently use the correct build system commands
- [x] TaskHelper completion works via CLI without shell-escaping issues
- [x] Task description appears in spec_writer instructions.md
- [x] `pixi run check` passes (897 tests)
- [x] E2E: agents use `pixi run` and complete tasks without env friction

---

### PR B3: Role-specific workflow fixes

- Branch: TBD

**Problem:** Two issues observed during E2E pipeline runs:

**No-commit PR failure (test_reviewer):**
- Review-only roles that produce no code changes can't create a PR because
  branch HEAD == integration HEAD. The agent worked around it by committing
  an unrelated pixi.lock change.
- Fix: allow review-only tasks to complete without a PR, or have the
  reviewer write a review summary file so there's always a commit.

**Git push without upstream (test_writer):**
- First `git push` fails because the task branch has no upstream set.
  Agent recovers with `git push -u`, but this is avoidable friction.
- Fix: set `push.autoSetupRemote=true` in worktree git config during task
  preparation, or configure upstream when creating the branch.

**Acceptance criteria:**
- [ ] Review-only tasks complete without needing a dummy commit
- [ ] First `git push` succeeds without `--set-upstream`
- [ ] `pixi run check` passes
- [ ] E2E: pipeline runs without either workaround

---

### PR B4: Ops concerns

- Branch: TBD

**Problem:** Agents encounter operational issues (wrong build commands,
missing dependencies, import path confusion) that are distinct from design
concerns. Even when the agent works around the problem, the friction should
be captured so it can be fixed systematically.

**Implementation:**
- Add `record_ops_concern()` to TaskHelper (or a `category` param on
  `record_concern`)
- Separate storage: `ops_concerns.log` in signal dir (or tagged lines in
  `concerns.log`)
- Orchestrator reads and surfaces ops concerns separately from design
  concerns in console output and integration PR body
- Update workflow footer to tell agents about ops concerns

**Acceptance criteria:**
- [ ] Agents can record ops concerns via TaskHelper
- [ ] Ops concerns appear separately from design concerns in console output
- [ ] Ops concerns appear in integration PR body
- [ ] `pixi run check` passes
- [ ] E2E: agent records an ops concern, visible in output

---

### PR BN: Concern instruction refinement

- Branch: TBD

> **Prerequisite:** PR B2 (clean runs needed for reliable experimentation)

**Problem:** With baseline instructions, agents do not organically raise
concerns about spec inconsistencies. Need to determine what level of guidance
produces reliable concern discovery across models.

**Approach:**
- Experiment with concern qualification guidance levels (minimal, prompted,
  checklist) in the implementer task description
- Test across models (Haiku, Sonnet, Opus) with the roles/pipeline.yaml
  graph and its deliberate BoundedQueue spec inconsistency
- Settle on a default guidance level that works with "reasonable agents"
- May feed into the structured instruction architecture (see BACKLOG.md)

**Acceptance criteria:**
- [ ] All four roles produce expected artifacts (specs, tests, implementations)
- [ ] At least one guidance level reliably produces organic concern discovery
- [ ] Results matrix (model × guidance level) documented in graphs/roles/README.md
- [ ] Guidance is documented and integrated into templates or protocol
- [ ] `pixi run check` passes

---

### PR C: failure/ and workstreams/ test graphs

- Branch: `test/failure-and-workstream-graphs`

**failure/ test graphs:**
- `graphs/failure/README.md` — category docs
- Agent .failed signal (impossible task)
- Blocked downstream task (A fails → B blocked)
- Retry with `max_task_attempts: 2`

**workstreams/ test graphs:**
- `graphs/workstreams/README.md` — category docs
- Diamond dependency (A→B, A→C, B→D, C→D)
- Same-workstream serialization (independent tasks, one workstream)

**Acceptance criteria:**
- [ ] Failed task produces correct outcome and error messages
- [ ] Blocked tasks emit task_blocked event
- [ ] Diamond topology executes correctly
- [ ] `pixi run check` passes
- [ ] E2E: at least one graph from each category runs

---

### PR D: Wire completion gates + gates/ test graphs

- Branch: `feat/completion-gates`

**Wire gate execution:**
- Add gate step to `StandardTaskRunner` (after completion signal, before merge)
- Run `completion_gate` shell command in the task's worktree
- On failure: retry up to `max_gate_attempts`, then mark task failed
- Emit step-level events: `task_gate_running`, `task_gate_passed`/`task_gate_failed`

**Test graphs:**
- `graphs/gates/README.md` — category docs
- Graph with `completion_gate: "pixi run test"` — verifies agent's code passes tests

**Acceptance criteria:**
- [ ] Gate command runs after task signals .done
- [ ] Gate failure triggers retry (up to max_gate_attempts)
- [ ] Gate pass proceeds to PR merge
- [ ] Verbose output shows gate events
- [ ] `pixi run check` passes
- [ ] E2E: gate graph runs, tests pass, PR merges

---

### PR E: Wire ADR production + adr/ test graphs

- Branch: `feat/adr-production`

**Wire ADR instructions:**
- Translate `_AdrPolicy` in the framework adapter into agent instructions
  that produce ADR files (e.g., append ADR writing directive to instructions.md
  when `adr_verbosity != none`)
- Define ADR output location and format

**Test graphs:**
- `graphs/adr/README.md` — category docs
- Graphs with `primary_agent.adr_verbosity` set to standard, detailed, educational
- Verify ADR files are produced with appropriate detail

**Acceptance criteria:**
- [ ] Agent produces ADR when verbosity != none
- [ ] ADR detail scales with verbosity level
- [ ] `pixi run check` passes
- [ ] E2E: adr graph runs, ADR file is present in committed output

---

### PR F: Agent-assisted integration branch merging (optional)

- Branch: `feat/agent-merge`

> **Decision gate:** Before starting this PR, evaluate whether to implement
> now or defer to `docs/BACKLOG.md`. The value depends on how often merge
> conflicts arise in practice and whether human review is sufficient.

**Merge strategy options (configurable per-graph or per-workstream):**
- `human` — Create integration PR, leave for human review and merge (default,
  current behavior after PR A2).
- `auto` — If no merge conflicts, merge automatically. If conflicts exist,
  leave for human review.
- `agent` — If merge conflicts, launch an agent to resolve them. Require
  the test suite to pass before completing the merge. If the agent cannot
  resolve, leave for human review.

**Implementation sketch:**
- Add a `merge_strategy` field to WorkstreamSpec or OrchestratorConfig.
- For `auto`: attempt `gh pr merge`, fall back to `human` on failure.
- For `agent`: create a tmux pane with Claude Code, instruct it to resolve
  conflicts and run tests. Agent uses TaskHelper-like API to signal success
  or failure.

**Acceptance criteria:**
- [ ] `human` mode creates PR without merging (same as PR A2)
- [ ] `auto` mode merges when clean, falls back when not
- [ ] `agent` mode resolves conflicts and runs tests before merging
- [ ] `pixi run check` passes
- [ ] E2E: test with conflicting parallel workstreams

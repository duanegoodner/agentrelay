# API Reference

Package-oriented API documentation for `agentrelay`.

Conventions used here:

- Package entries document each package `__init__.py` surface (`agent`, `task_graph`, `task_runtime`, `workstream`).
- Module pages are only listed when the module exposes symbols that are not part of the package-level exports.

# Context from prerequisite tasks

## write_greet_fn

Description: Write a Python function greet(name: str) -> str in demo_output/write_greet_fn/greet.py that returns the string "Hello, {name}!"

The files produced by this task are available in your worktree (already merged into main before your branch was created).
